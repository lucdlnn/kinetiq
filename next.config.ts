import type { NextConfig } from "next";

// Note: For advanced PWA offline support, next-pwa plugin can be added here.
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
